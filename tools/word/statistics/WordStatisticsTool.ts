import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { wordStatisticsProcessor } from './WordStatisticsProcessor'; export const wordStatisticsTool = { id: 'word-count', name: 'Word Count', description: 'Count words and estimate reading time in a DOCX.', category: 'utility', supportedDocumentTypes: ['docx'], execute: async (context: ToolExecutionContext) => wordStatisticsProcessor.process(context.file ?? context.files?.[0]!) } satisfies Tool;