import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { docxRepairProcessor } from './DocxRepairProcessor'; export const docxRepairTool = { id: 'docx-repair', name: 'Repair DOCX', description: 'Validate and safely repair limited DOCX package issues.', category: 'utility', supportedDocumentTypes: ['docx'], execute: async (context: ToolExecutionContext) => docxRepairProcessor.process(context.file ?? context.files?.[0]!) } satisfies Tool;