import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { rtfToDocxProcessor } from './RtfToDocxProcessor'; export const rtfToDocxTool = { id: 'rtf-to-docx', name: 'RTF to DOCX', description: 'Convert RTF documents to DOCX.', category: 'converter', supportedDocumentTypes: ['rtf'], execute: async (context: ToolExecutionContext) => rtfToDocxProcessor.process(context.file ?? context.files?.[0]!, context.options) } satisfies Tool;