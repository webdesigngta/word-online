import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { docxCompareProcessor } from './DocxCompareProcessor'; export const docxCompareTool = { id: 'docx-compare', name: 'Compare DOCX', description: 'Compare two DOCX documents and return structured differences.', category: 'utility', supportedDocumentTypes: ['docx'], execute: async (context: ToolExecutionContext) => docxCompareProcessor.process(context.files ?? []) } satisfies Tool;