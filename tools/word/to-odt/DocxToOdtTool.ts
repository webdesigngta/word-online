import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { docxToOdtProcessor } from './DocxToOdtProcessor'; export const docxToOdtTool = { id: 'docx-to-odt', name: 'DOCX to ODT', description: 'Convert DOCX documents to OpenDocument text.', category: 'converter', supportedDocumentTypes: ['docx'], execute: async (context: ToolExecutionContext) => docxToOdtProcessor.process(context.file ?? context.files?.[0]!, context.options) } satisfies Tool;