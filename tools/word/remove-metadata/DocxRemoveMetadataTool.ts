import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { docxRemoveMetadataProcessor } from './DocxRemoveMetadataProcessor'; export const docxRemoveMetadataTool = { id: 'docx-remove-metadata', name: 'Remove DOCX Metadata', description: 'Clear document metadata without changing document content.', category: 'utility', supportedDocumentTypes: ['docx'], execute: async (context: ToolExecutionContext) => docxRemoveMetadataProcessor.process(context.file ?? context.files?.[0]!, context.options) } satisfies Tool;