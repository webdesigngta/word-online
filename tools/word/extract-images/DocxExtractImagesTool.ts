import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { docxExtractImagesProcessor } from './DocxExtractImagesProcessor'; export const docxExtractImagesTool = { id: 'docx-extract-images', name: 'Extract DOCX Images', description: 'Extract original images from a DOCX package.', category: 'utility', supportedDocumentTypes: ['docx'], execute: async (context: ToolExecutionContext) => docxExtractImagesProcessor.process(context.file ?? context.files?.[0]!) } satisfies Tool;