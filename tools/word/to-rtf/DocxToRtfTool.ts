import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { docxToRtfProcessor } from './DocxToRtfProcessor'; export const docxToRtfTool = { id: 'docx-to-rtf', name: 'DOCX to RTF', description: 'Convert DOCX documents to rich text format.', category: 'converter', supportedDocumentTypes: ['docx'], execute: async (context: ToolExecutionContext) => docxToRtfProcessor.process(context.file ?? context.files?.[0]!, context.options) } satisfies Tool;