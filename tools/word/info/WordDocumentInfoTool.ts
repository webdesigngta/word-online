import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { wordDocumentInfoProcessor } from './WordDocumentInfoProcessor'; export const wordDocumentInfoTool = { id: 'word-document-info', name: 'Word Document Info', description: 'Inspect DOCX metadata and document statistics.', category: 'utility', supportedDocumentTypes: ['docx'], execute: async (context: ToolExecutionContext) => wordDocumentInfoProcessor.process(context.file ?? context.files?.[0]!) } satisfies Tool;