import type { Tool } from '../../../core/tool-registry/Tool'; import type { ToolExecutionContext } from '../../../core/tool-execution/ToolExecutionContext'; import { odtToDocxProcessor } from './OdtToDocxProcessor'; export const odtToDocxTool = { id: 'odt-to-docx', name: 'ODT to DOCX', description: 'Convert OpenDocument text files to DOCX.', category: 'converter', supportedDocumentTypes: ['odt'], execute: async (context: ToolExecutionContext) => odtToDocxProcessor.process(context.file ?? context.files?.[0]!, context.options) } satisfies Tool;