# Week 3 DOCX compatibility gate

The automated browser gate runs the internal noindex 20-case compatibility suite and fails below 95% upload/import → edit → DOCX export → reopen success.

Run through `.github/workflows/word-compatibility.yml`.
